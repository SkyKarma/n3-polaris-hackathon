import { AmountConverterPipePipe } from './amount-converter-pipe.pipe';

describe('AmountConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AmountConverterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
